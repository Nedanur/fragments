# docker-compose.yml
services:
  fragments:
    image: ${DOCKER_REPO}:${DOCKER_TAG}
    init: true
    ports:
      - '8080:8080'
    environment:
      - NODE_ENV=production
      - HTPASSWD_FILE=./.htpasswd